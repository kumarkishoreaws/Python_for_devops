text = "   Some spaces around   "
stripped_text = text.strip()
print("Stripped text:", stripped_text)

#Stripped text = text without leading and trailing spaces.
#Stripped text means text with all unnecessary spaces (whitespace) removed.